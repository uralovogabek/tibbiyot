import 'package:flutter/material.dart';

class AppColors {
  // Asosiy brend ranglari
  static const Color primary = Color(0xFF10367D);
  static const Color primaryLight = Color(0xFF1E499D);
  static const Color primarySoft = Color(0x1410367D);

  static const Color accent = Color(0xFFA5CE00);
  static const Color accentDark = Color(0xFF759400);
  static const Color accentSoft = Color(0x2EA5CE00);

  // Skrinshotdagi neytral minimalist fon
  static const Color background = Color(0xFFF3F4F8);
  static const Color surface = Colors.white;
  static const Color surfaceMuted = Color(0xFFF8FAFC);

  // 2x2 vidjet ranglari
  static const Color widgetMauve = Color(0xFF93789E);
  static const Color widgetPeach = Color(0xFFECA074);
  static const Color widgetGreen = Color(0xFF82B766);
  static const Color widgetCoral = Color(0xFFE56D67);

  // Matnlar
  static const Color textPrimary = Color(0xFF1E2432);
  static const Color textSecondary = Color(0xFF6E788B);
  static const Color textMuted = Color(0xFF9CA5B4);
}

class AppRadius {
  static const double card = 24.0;
  static const double dock = 36.0;
  static const double pill = 999.0;
}
